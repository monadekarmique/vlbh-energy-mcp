name: Swift CI

on:
  push:
    branches: [main]
    paths:
      - 'Package.swift'
      - 'Sources/**'
      - 'Tests/**'
  pull_request:
    branches: [main]
    paths:
      - 'Package.swift'
      - 'Sources/**'
      - 'Tests/**'

jobs:
  build-and-test:
    name: Build & Test
    strategy:
      matrix:
        include:
          - xcode: '15.4'
            runner: macos-14
          - xcode: '16.2'
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode ${{ matrix.xcode }}
        run: sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app

      - name: Show Swift version
        run: swift --version

      - name: Resolve dependencies
        run: swift package resolve

      - name: Build
        run: swift build -v

      - name: Run tests
        run: swift test -v

  build-ios:
    name: Build for iOS
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      # Xcode par défaut de l'image : épingler Xcode_16.2 sur macos-15 échouait
      # depuis le 07.04.2026 (« iOS 18.2 is not installed » — le runtime du
      # simulateur n'est plus livré avec l'image). Destination générique : pas
      # de nom d'appareil ni de version d'OS à maintenir.
      - name: Build for iOS Simulator
        run: |
          xcodebuild build \
            -scheme VLBHEnergyKit \
            -destination 'generic/platform=iOS Simulator' \
            -skipPackagePluginValidation

  lint:
    name: Swift Format Check
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Check Swift compilation
        run: swift build 2>&1 | head -50 || true

      - name: Verify package structure
        run: swift package describe

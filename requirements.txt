fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic>=2.9,<3
pydantic[email]
python-dotenv==1.0.1
fastapi-mcp>=0.3.4
# fastapi-mcp 0.4.0 calls Server(name, description); mcp 2.x dropped that
# signature. Render (Python 3.14) resolves mcp 1.27.2 and runs; CI (3.11)
# resolved 2.2.0 and has been red since 17.08.2026. Pin what production runs.
mcp>=1.12.0,<2
jinja2==3.1.4
# --- iTherapeut 6.0 ---
supabase>=2.0.0
qrbill>=1.1.0
weasyprint>=62
# --- tests ---
pytest>=8.0
pytest-asyncio>=0.24
